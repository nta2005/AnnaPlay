<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nta.annaplay">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.HelloActivity2" />
        <activity android:name=".activity.PlayNhacActivity" />
        <activity android:name=".activity.AlbumActivity" />
        <activity android:name=".activity.TheLoaiChuDeActivity" />
        <activity android:name=".activity.ChuDeActivity" />
        <activity android:name=".activity.PlayListActivity" />
        <activity android:name=".activity.BaiHatActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.HelloActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>